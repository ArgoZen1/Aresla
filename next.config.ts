// next.config.js
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // ... autres configurations

  // Suppression complète de la configuration images.domains
};

export default nextConfig;
